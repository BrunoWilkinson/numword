#ifndef NUMWORD_H
#define NUMWORD_H

#include <cstdio>
#include <string>

namespace core {
    class numword {
        public:
            std::string words(uint64_t n);
    };
};

#endif // NUMWORD_H